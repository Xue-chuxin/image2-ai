# 更新日志

本项目遵循面向开源自部署用户的发布记录。维护者发布新版本时，请把用户需要知道的功能变化、修复、数据库迁移和环境变量变化写在这里。

## 未发布

- 前台新增暗色模式：顶栏主题按钮支持「浅色 / 暗色 / 跟随系统」三态切换，本地记忆并防止刷新闪烁，桌面与移动端均可用。
- 生成任务轮询在离开创作页或重新提交时自动停止，不再在后台持续请求。
- 控制台系统设置页首次加载显示骨架屏，取代空白等待。

- 性能：后台配置读取增加内存缓存（30 秒），大幅减少每次页面请求对配置表的重复查询；控制台设置页与全站首屏加载更快。修改配置后即时生效。
- 性能：提示词润色不再为读取一个配置项而执行完整的后台诊断（含数据库探测）。
- 性能：后台用户列表的统计计数改为批量聚合查询，用户较多时打开更快（原先每个用户单独查 4 次）。
- 性能：生成图与参考图的回源支持长效缓存与 304 协商缓存，重复浏览走浏览器/磁盘缓存，减轻服务器图片流量。

- 安全：修复"未设置密码的账号可被任意密码接管"的问题——此类账号改为拒绝密码登录，并收紧管理员初始化逻辑，不再静默提权已注册的普通用户或覆盖其密码。
- 安全：API 错误响应不再把数据库/内部异常原文透给前端，仅透传业务提示，其余返回通用文案并记服务端日志。
- 安全：支付凭证文件（storage 的 payments 目录）改为需要本人或管理员会话才能访问，未授权返回 404；系统自检新增"凭证是否暴露在 public 目录"的检查项。
- 支付：修复订单超时过期后收到成功支付却"钱付了积分不到账"的问题——过期订单迟到支付现在可正常入账；已取消订单收到支付会拦截并记诊断供人工处理。
- 支付：取消订单改为原子操作，消除与支付回调竞态时把已支付订单误标为已取消的可能；取消已支付订单会明确提示无法取消。
- 积分：修复冻结积分在重复结算/退款时可能被减为负数或双倍返还的问题（条件更新 + 任务失败状态守卫）。

## v0.2.0 - 2026-07-06

本版本为大规模 UI 重构：前台换新为浅色侧边栏应用布局，用户中心与管理后台迁移到基于 Vben Admin 的控制台。升级前请阅读 UPGRADE.md 对应章节。

- 管理后台整体迁移到控制台：运营概览（实时统计）、系统设置（六页签全量配置）、用户管理、任务管理、图片管理（生成作品+运营精选）、上传管理、账单管理（支付渠道/套餐/订单）、支付诊断、系统自检、中转站推荐，均基于现有 /api/admin/* 接口。
- 下线 Next.js 旧版 /admin 页面与 TDesign 依赖（/admin 自动跳转控制台，包体积显著减小）；管理员登录并入控制台统一登录页。
- 新增 GET /api/admin/dashboard 与 GET /api/admin/relays 接口。
- 控制台上线用户中心：积分总览、在线充值（渠道选择/扫码支付/自动到账轮询）、充值订单管理（继续支付/手动查单/取消）、积分流水、账号安全（改密），前台 /account 及顶栏充值入口改为跳转控制台。
- 新增 /api/console/billing/* 与 /api/console/user/password 接口；新增按用户查询积分流水的能力；控制台计费接口同时接受管理员与普通用户会话。
- 生产环境控制台静态产物由 Next.js 托管在 /console 路径（新增 npm run build:console 构建命令）。
- 新增基于 Vben Admin 5 (Vue 3 + Ant Design Vue) 的控制台应用（console/ 目录），用于承载用户中心与新版管理后台；登录与前台账号互通（HTTP-only Cookie 会话，前台已登录时免登录进入）。
- 新增 /api/console/* 适配接口（登录/登出/用户信息/权限码），普通用户与管理员按角色显示控制台菜单。
- 前台整体重构为浅色侧边栏应用布局（左侧导航 + 顶部搜索/积分/账户），首页改为"画廊广场"（推广 banner + 应用广场 + 作品瀑布流），视觉风格全面更新。
- 新增应用中心、智能助手、更多工具三个占位页面，展示后续规划能力，导航先行对齐完整形态。
- 下线旧版 glass_app / tdesign_workspace 双模板体系，后台"前台模板"配置不再影响前台展示（后台自身样式不受影响）。
- 顶栏新增全局作品搜索（跳转首页作品区检索）、积分余额与充值快捷入口。
- 修复"再次生成"丢失画幅比例、质量、张数、英文提示词和负面词参数的问题，现在会完整回填到创作页。
- 修复账户下拉菜单无法点击外部关闭的问题。
- 旧灵感页 /prompts 并入首页画廊广场（自动跳转），移除无入口的 /prompts/[slug] 示例详情页。
- 清理前台死代码与未使用依赖（react-bits 动效组件、motion/gsap/ogl），globals.css 由 6388 行精简到约 440 行，后台样式隔离到 admin-legacy.css。

## v0.1.4 - 2026-06-20

- 后台新增首页弹窗配置，可开启/关闭并填写 Markdown 或 HTML5 公告内容，首页会弹窗展示。
- 修复 Docker 构建时 TDesign 邮箱输入类型和友情链接解析的 TypeScript 报错。
- 修正 Docker Compose 图片 volume 挂载路径，使默认本地存储持久化到 `/app/public/storage/generated` 和 `/app/public/storage/uploads`。
- 升级脚本新增旧版图片目录备份和回填，避免从旧 Compose 路径升级时丢失容器内 `/app/public/storage` 图片。
- 修复创作页移动端生成失败后错误提示、选项控件和底部导航可能错位或溢出的问题。
- 修复创作页桌面端失败态右侧结果预览栏因 sticky 定位导致与左侧表单错位的问题。
- 完善 GitHub 开源展示材料，新增 README 首屏预览图、Social Preview、Topics 建议和开源传播清单。
- 新增贡献指南、安全策略、支持说明、行为准则、Issue 模板、PR 模板和部署求助模板。
- 新增英文 README、常见问题、Release 模板和 Good First Issue 候选清单，降低新用户试用和贡献门槛。

## v0.1.3 - 2026-06-18

- 后台新增中转站推荐一级菜单，并保留明心 AI、极狗中转站两个 Logo 跳转入口。
- 后台移动端改为左侧抽屉导航和右侧用户入口，优化统计卡、用户表、任务表和作品表在手机端的排版。
- 修复后台 TDesign 组件水合错误，避免下拉箭头在服务端和客户端渲染 class 不一致导致控制台报错。
- 修复后台移动端表格操作列固定、积分输入控件错位、用户表内容被遮挡等问题。
- 优化创作页移动端生成后的布局，避免 DeepSeek 整理结果和底部导航发生遮挡。
- 调整创作页桌面端布局，将任务信息和积分说明移动到创作表单下方，手机端展示顺序保持不变。
- 优化前台首页背景、Logo/Favicon 展示、能力卡片和页脚在桌面及手机端的视觉表现。

## v0.1.2 - 2026-06-18

- 优化 TDesign 企业官网首页首屏，增加产品工作台预览和作品结果宫格。
- 新增首页作品结果预览带，公开作品不足时自动使用内置提示词示例兜底。
- 将产品能力区调整为“一大三小”结构，强化从中文描述到视觉资产沉淀的主张。
- 优化适用场景区，改为“适合谁 / 输入 / 产出”的企业官网表达。

## v0.1.1 - 2026-06-18

- 新增前台模板切换能力，支持 `TDesign 企业官网风` 和旧版 `玻璃拟 App 风` 在后台切换。
- 将 TDesign 前台首页调整为企业官网首页，用户功能页继续保留应用工作台结构。
- 丰富企业官网首页内容，增加产品可信能力、适用团队、生产流程、运营能力、场景矩阵和底部转化区。
- 拆分普通用户登录、普通用户注册和管理员登录入口，新增 `/signup` 与 `/admin/signin`。
- 前台充值支持展示所有已启用且配置完整的支付渠道，并使用后台自定义显示名称。
- 普通用户新注册需要邮箱验证码，已有用户登录和管理员登录不受影响。
- 注册验证码增加 60 秒发送冷却，避免同一邮箱频繁发送。
- 支付宝当面付兼容裸应用私钥/支付宝公钥格式，避免创建订单时暴露 OpenSSL 内部错误。
- 支付宝当面付待支付订单增加主动查单兜底，异步回调未到达时也可在刷新状态后自动入账，并记录支付诊断事件。
- 优化支付宝当面付主动查单体验，新订单前 2 分钟更快确认，后台诊断区分自动查单与手动查单。
- 支付宝当面付下单后增加服务端短轮询确认，用户离开用户中心页面后仍会在短时间内自动查单入账。
- 优化用户中心充值订单轮询，订单未变化时不再反复替换二维码区域，降低二维码闪烁。
- 用户中心待支付订单自动刷新时额外拉取单笔订单状态，并在页面重新可见时立即刷新，提升到账状态同步速度。
- 用户中心账单状态接口和前端轮询增加防缓存处理，避免 CDN 或浏览器复用旧的待支付状态。
- 修复用户中心自动刷新后刚创建的充值订单可能从前台列表消失的问题。
- 新增 `UPGRADE.md`，说明源码部署、Docker Compose 部署和二开用户的升级流程。
- 新增 `scripts/update.sh`，提供源码部署、完整 Docker Compose、仅 Web Docker Compose 三种常用升级入口。
- 更新 README，补充升级说明、版本发布建议，并修正 Docker Compose 使用 `.env.production` 的命令写法。

## v0.1.0 - 2026-06-17

- 开源第一版，包含中文 AI 生图 WebUI、提示词库、DeepSeek 提示词润色、生图任务、作品历史、积分账户、后台配置和 Docker Compose 部署基础。
- 默认生图 Provider 为 `openai`，通过统一后端接口调用模型能力。
- `chatgpt_web` Provider 仅作为实验性预留，未配置时返回明确的 disabled 错误。
