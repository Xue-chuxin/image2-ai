# 不在此硬编码 registry，默认使用官方源 https://registry.npmjs.org。
# 国内网络可在构建/安装环境自行覆盖，例如：
#   - 环境变量：npm_config_registry=https://registry.npmmirror.com
#   - 或全局：pnpm config set registry https://registry.npmmirror.com
# pnpm-lock.yaml 只存 integrity 哈希、不锁定 registry 域名，切换镜像不影响校验。
