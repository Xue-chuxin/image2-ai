VITE_BASE=/console/

# 接口地址（生产与 Next.js 同域）
VITE_GLOB_API_URL=/api

# 是否开启压缩，可以设置为 none, brotli, gzip
VITE_COMPRESS=none

# 是否开启 PWA
VITE_PWA=false

# vue-router 的模式（hash 模式无需服务端路由回退）
VITE_ROUTER_HISTORY=hash

# 是否注入全局loading
VITE_INJECT_APP_LOADING=true

# 打包后不生成dist.zip
VITE_ARCHIVER=false
